pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'building docker image'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploying to target'
      }
    }

  }
}